# 紫城服饰仓库管理系统 - 部署教程

## 准备工作

| 需要的东西 | 去哪里弄 |
|-----------|---------|
| Node.js | https://nodejs.org → 下载 Windows LTS 版 → 安装 |
| MySQL | 不用装，小皮面板自带 |
| 小皮面板 | 你已经有了 |

---

## 第一步：创建数据库

1. 打开**小皮面板** → 点 **MySQL** 旁边的 **启动**
2. 点 **MySQL** → **管理** → 会打开数据库管理页面
3. 新建数据库，名字随便，比如 `schema`（字符集选 `utf8mb4_general_ci`）
4. 点进刚创建的数据库 → 顶部点 **导入** → 选择 `schema.sql` → 执行

---

## 第二步：配置并启动后端

1. 把 `warehouse-backend-v4.0.zip` 解压到你想要的位置，比如 `D:\warehouse-server\`

2. 打开解压出来的 `.env` 文件，填你的数据库信息：
```
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=你的数据库用户名
DB_PASSWORD=你的数据库密码
DB_NAME=你的数据库名
JWT_SECRET=随便打一串乱码
PORT=3001
```

3. 打开命令行，进入后端目录：
```bash
cd D:\warehouse-server
npm install --omit=dev
node dist/index.js
```
看到 `仓库管理系统 API 已启动: http://localhost:3001` 就是成功了。

4. **添加管理员账号**（重要！第一次部署必须做）：

命令行先进到后端目录，然后运行：
```bash
cd D:\warehouse-server
node seed.js
```
看到 `管理员账号已创建: admin / admin123` 就行了。

> 注意：`seed.js` 里的数据库密码要和 `.env` 里的一致。如果不一致，用记事本打开 `seed.js` 改一下。

---

## 第三步：部署前端

1. 把 `warehouse-frontend-v4.0.zip` 解压到任意目录，比如 `D:\www\warehouse\`

2. 小皮面板 → **网站** → 创建网站：
   - 域名：填你服务器的 IP 地址（或已绑定的域名）
   - 根目录：选 `D:\www\warehouse\`（刚解压的目录）
   - PHP 版本：选 `纯静态`

3. 点刚创建的网站 → **管理** → **伪静态**，粘贴：
```nginx
location /api/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_set_header Host $host;
}
location / {
    try_files $uri $uri/ /index.html;
}
```
保存，重启网站。

---

## 第四步：测试

用手机或电脑浏览器打开 `http://你的服务器IP`，看到登录页就是成功了。

**管理员登录**：`admin` / `admin123`

---

## 以后只用启动后端就行了

每次服务器重启后，需要重新启动后端：

```bash
cd D:\warehouse-server
node dist/index.js
```

如果想让它一直在后台跑（推荐），装个 pm2：

```bash
npm install -g pm2
pm2 start dist/index.js --name warehouse-api
pm2 save
pm2 startup   # 设置开机自启
```

以后用 `pm2 status` 看状态，`pm2 restart warehouse-api` 重启。

---

## 文件清单

| 文件 | 干什么的 |
|------|---------|
| `warehouse-frontend-v4.0.zip` | 前端页面，解压放 nginx |
| `warehouse-backend-v4.0.zip` | 后端服务，解压后用 Node.js 跑 |
| `schema.sql` | 数据库建表语句（在后端 zip 里） |
| `seed.js` | 创建管理员账号的脚本（在后端 zip 里） |
| `.env` | 数据库连接配置（在后端 zip 里，部署时要改） |
